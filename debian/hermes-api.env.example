# Merge into the existing profile's .env; do NOT replace that file.
API_SERVER_ENABLED=true
# Generate your own value. Never use the placeholder as a real key.
API_SERVER_KEY=REPLACE_WITH_A_RANDOM_SECRET
